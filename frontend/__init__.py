"""Streamlit frontend package.

`presenter` holds the pure display logic and is safe to import anywhere.
`app` executes Streamlit page setup at import time, so it is only ever run
through `streamlit run frontend/app.py`.
"""
