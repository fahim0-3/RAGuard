# Compatibility entry point for pip users. The canonical dependency metadata
# is pyproject.toml and the exact resolution is uv.lock. This installs only the
# API/model runtime; use `uv sync --locked --all-groups` for development.
-e .
