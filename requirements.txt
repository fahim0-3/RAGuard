# ---------------------------------------------------------------------------
# RAGuard dependencies
# Tested against Python 3.11 / 3.12. See README "Python version" note before
# using 3.13+ (PyTorch wheel availability lags new CPython releases).
# ---------------------------------------------------------------------------

# --- Configuration ---
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Orchestration (LCEL) ---
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3
langchain-google-genai>=2.0

# --- LLM providers (swappable via LLM_PROVIDER) ---
google-genai>=1.0
# Ollama is reached over HTTP by langchain-ollama; install only when needed.
# langchain-ollama>=0.2

# --- Embeddings and reranking ---
sentence-transformers>=3.0
torch>=2.2
transformers>=4.41

# --- Lexical retrieval ---
rank-bm25>=0.2.2

# --- Vector store ---
psycopg[binary]>=3.1
psycopg-pool>=3.2
pgvector>=0.3

# --- Service layer ---
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# --- Frontend ---
streamlit>=1.36

# --- Evaluation ---
pytest>=8.2
pytest-asyncio>=0.23
ragas>=0.2
datasets>=2.20

# --- Tooling ---
ruff>=0.5
